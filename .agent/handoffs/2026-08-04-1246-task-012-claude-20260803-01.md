# TASK-012 交接归档

状态：DONE
负责人：claude-20260803-01
完成时间：2026-08-04 12:46 +0800

## 完成内容

- OP3-B 前向兼容基础（零行为变更）：
  - `src/core/version.ts` 导出 `FACTORY_VERSION = '0.1.0'`。
  - `agent-schema.ts` 导出 `CURRENT_AGENT_CONFIG_SCHEMA_VERSION = 1`。
  - `agents.ts` `readAgentConfig(raw, version)` 版本化只读 reader：v1=identity（`agentConfigSchema.parse`），不原地 mutate；未知版本抛 `VALIDATION_ERROR` 并提示未来 `agentctl migrate`。`loadPortableConfig` 显式按 `schema_version ?? 1` 分派。
  - `backup-schema.ts` 加性字段 `factory_version: z.string().default('')`；`backup.ts` 写入 `FACTORY_VERSION`；旧备份（无该字段）恢复时默认 `''`。
  - `trash-schema.ts` `components` 由 `.length(6)` 改 `.min(6)`，为未来第 7 类组件留前向兼容。
- OP3-C adapter 治理（B1 最小）：
  - `RuntimeAdapter` 增 `buildEnv(agent, source?)`；Claude/Codex adapter 实现（`buildSafeBaseEnvironment` + `CLAUDE_CONFIG_DIR`/`CODEX_HOME`），`context()` 改用 `this.buildEnv`。
  - `runtime.ts` `buildRuntimeEnvironment` 委托 `getRuntimeAdapter(agent).buildEnv`，删除 provider if/else。
  - `getRuntimeAdapter` 改 `Record<RuntimeProvider, () => RuntimeAdapter>` 工厂对象（编译期穷尽，等价 assertNever）；未知 provider 抛 `DEPENDENCY_MISSING`，不再静默回退 Codex（修 T-2）。
- 明确不在本批：AIEF2 备份格式 negotiate、`agentctl migrate` 命令、RuntimeAdapter 扩 7 方法、CC Switch 同步入 adapter。

## 验证记录

- `npm run verify`：build 通过、108 项单测通过、lint 通过。
- `npm run test:e2e`：1 项通过。
- 新增 tests/agents.test.ts(+2)、runtime +2、backup-restore +2、trash +1。

未 push（按规则任务完成即 commit，push 仍需用户明确授权）。
