# TASK-011 交接归档

状态：DONE
负责人：claude-20260803-01
完成时间：2026-08-04 12:35 +0800

## 完成内容

- R7：扩展备份 `shouldCopy` 黑名单（settings.json/config.json/.netrc/credentials.json/gcloud.json/id_rsa/id_ed25519/id_dsa/id_ecdsa + .pfx/.keystore 扩展名），SSH 私钥 id_\* 排除但 id_\*.pub 保留；新增 `SECRET_PATTERN`（AKIA/sk-/api_key=… 等）。
- R27：新增 `rejectSecretsInStage`，在打包前扫描 workspace+runtime 全部暂存文件（含未跟踪），命中 Secret 模式即拒绝；`rejectTrackedSecrets` 复用同一 pattern。
- R8：解密产物 `writeFile` 以 `0o600` 落盘。
- R20：`TrashService.purgeOne(trashId,{force,dryRun})` 支持强制清理 failed/moving/purging 态；doctor `trash-health` 告警扩到 failed/moving/purging 且 remediation 提示 `agentctl trash purge --force <id>`；CLI `trash purge [trash-id]` 配 `--force`。
- R21：`verifyChecksums` 增加集合一致性校验，拒绝 manifest 未声明文件（manifest.yaml/checksums.txt 因备份时序豁免）。
- R5：`factoryConfigSchema`+`readConfig`（zod，`sync.sanitize_non_whitelist` default false）；`syncCcSwitchClaudeProvider` 增 `sanitizeNonWhitelist` 形参，开启时移除员工 settings.json 残留非白名单 env；`prepareRuntime` 经 `readConfig` 读取开关并下发。

## 验证记录

- `npm run verify`：build 通过、101 项单测通过、lint 通过。
- `npm run test:e2e`：1 项通过。
- 新增 tests/config.test.ts(+4)、backup-restore +3、trash +3、doctor +1、runtime +1。
- 未读取或输出任何真实凭据值；测试均使用占位 Secret 串。

未 push（按规则任务完成即 commit，push 仍需用户明确授权）。
