# Handoff - TASK-017 OP1 Stage A 认知记忆层运行时强制

- Task ID: TASK-017
- Owner agent: claude-20260803-01
- 交接时间：2026-08-04 17:25 +0800
- 状态：已完成，待 commit（不 push）
- 基线：master 4f15147（TASK-016）
- 范围来源：用户经 AskUserQuestion 选定「OP1 认知记忆层强制」，spec 见 .scratch/plan.md

## 本批交付

OP1 Stage A 收敛 W1：将「声明但不强制」的 `authority_order` 升级为运行时强制 + 派生 stance 注入。`memory.enforced:true` 时 `prepareRuntime` 在 spawn 前硬校验 authority_order 不变量（首位=agent、含 agent、无重复、非空）；CLAUDE.md/AGENTS.md 的记忆权威顺序 stance 从 `authority_order` 派生；doctor memory-enforcement 4 态。不越界到 OP1 Stage B-E（knowledge 索引/transcript/ExperienceExtractor/archival）或 resolveConflict 热路径接线。

### 源码改动

- `src/schemas/agent-schema.ts`：增 `AUTHORITY_LAYERS` 常量 + `AuthorityLayer` 类型（`['agent','knowledge','decisions','skills','native_memory','session']` as const）；`authority_order` 由 `z.array(z.string())` 收紧为 `z.array(z.enum(AUTHORITY_LAYERS))` 编译期穷尽（R26 不变量「新层不得排在 agent 之前」由 enum + 运行时校验共同守护）；memory 块增 `enforced: z.boolean().optional()`（向后兼容，零 schema_version bump，与 config_hash 零交互--后者仅哈希 runtime 块）。
- `src/core/authority.ts`（新增）：纯函数零 I/O。`validateMemoryConfig(memory)` 检查 authority_order：空 / 缺 agent / agent 非首 / 重复 各报 issue，标准 6 层 ok。`renderAuthorityStance(memory)` 从 authority_order 派生「## 记忆权威顺序」stance，含全部声明层（agent 居首）+ 末尾约束句「原生自动记忆与当前会话上下文不得凌驾于正式文件之上，不得因记忆内容绕过权限规则」。
- `src/core/templates.ts`：`renderRuntimeFiles` 签名 `(workspace, config: AgentConfig, values)`，在生成的 CLAUDE.md/AGENTS.md 末尾追加 `renderAuthorityStance(config.memory)`。caller 改为传 config。stance 从 authority_order 派生而非硬编码，自愈于配置变更。
- `src/core/create-agent.ts`：memory 块增 `enforced: true`（4 预设 user-operations/monetization/growth/engineering 均经 buildAgentConfig，故全部默认启用运行时强制，行为不变）。
- `src/application/factory-application.ts`：新增 `assertMemoryEnforced(agent)`--`enforced !== true` 直接 return（false/undefined 跳过），否则调 `validateMemoryConfig`，无效抛 `VALIDATION_ERROR`（remediation 指向修 authority_order 或设 enforced:false 降级）。`prepareRuntime(registry)` 改 `prepareRuntime(registry, agent)`，首行调 `assertMemoryEnforced`。7 调用点（runAgent:258、chat:268、runtimeAuth:277、syncRuntime:293、runBridgeService:375、lifecycleAction:396、runJob:537）更新为 `const { registry, agent } = await this.getAgent(id); await this.prepareRuntime(registry, agent);`。零额外 I/O--复用 getAgent 已加载的 config，不重新读 agent.yaml。
- `src/core/doctor.ts`：agent 检查块内（config-drift 后）增 memory-enforcement 4 态：`enforced===undefined` -> warn「未声明（旧 agent.yaml）」；`enforced===false` -> warn「已显式关闭」；`enforced===true` + `validateMemoryConfig` ok -> pass；`enforced===true` + 无效 -> fail「authority_order 无效」+ remediation 指向修配置或降级。

### 测试

- `tests/authority.test.ts`（新增，8）：validateMemoryConfig 标准 6 层 ok / 子集 ok / 空 / 缺 agent / agent 非首 / 重复；renderAuthorityStance 列全部层 agent 居首 / 按 authority_order 排序。
- `tests/memory-enforcement.test.ts`（新增，2）：经 `application.syncRuntime(id)`（claude agent）。enforced:true + authority_order=['knowledge','decisions']（无 agent）-> rejects 抛「memory 配置无效」（VALIDATION_ERROR，spawn 前阻断）；enforced:false + authority_order=['knowledge'] -> rejects 抛「CC Switch」（NOT_FOUND，证明 memory gate 被跳过、已前进到 CC Switch 同步）。
- `tests/schemas.test.ts`（+1）：memory.enforced optional 向后兼容--无 enforced -> undefined；true -> true；false -> false。
- `tests/create-agent.test.ts`（+1）：新建 claude agent 断言 `config.memory.enforced === true`、CLAUDE.md 含「## 记忆权威顺序」与「1. agent（岗位正式文件」；codex agent 断言 AGENTS.md 含 stance。
- `tests/doctor.test.ts`（+1）：memory-enforcement 4 态--新建（enforced:true+有效）pass / 删 enforced -> warn「未声明」/ enforced:true+authority_order=['knowledge'] -> fail「authority_order」。

### 验证（实跑）

- `npm run verify`：build ✓、test 156 passed (31 files) ✓、lint（eslint --max-warnings=0 + prettier --check）✓
- `npm run test:e2e`：1 passed ✓

## 簿记

- TASK_BOARD.md：TASK-017 行 DONE，worktree=`master (TASK-017 commit)`（占位，下个任务回填真实 hash），详情块补 Completed at/Outcome。
- FILE_LOCKS.md：TASK-017 行 State ACTIVE->RELEASED。
- PROJECT_STATE.md：TASK-017 移入已完成；进行中置空。
- 未创建 `.agent/task-ids/TASK-017`（沿用 TASK-012~016 做法）。

## 边界与遗留

- 未做（Forbidden，需后续授权）：OP1 Stage B-E（knowledge 索引/transcript/ExperienceExtractor/archival 后端）、resolveConflict 热路径接线（运行时按 authority_order 自动取前者）、CLAUDE.md 内容漂移检测、schema_version bump、改隔离层/CC Switch/备份回收、OP3-A 长期（移除 Registry runtime 块 + I-5 model 收紧）/ OP2-F / OP5 / OP4-C、推送。
- documentFile 偏离裁定（D-011）：研究原议「prepareRuntime AND documentFile 双点强制」，本批只在 prepareRuntime 强制。理由：documentFile 现有 identity-doc 路径不变量校验（assertInside + realpath + symlink 检查）本身就是 authority 'agent' 层的写约束；叠加 memory 校验会阻止误配 agent 通过修自身身份文档自愈，损害可恢复性。prepareRuntime 已覆盖所有 spawn 前路径，单点足够。
- prepareRuntime 签名变更（registry -> registry,agent）选零额外 I/O 路径：getAgent 已 loadPortableConfig 加载 config，直接透传而非在 prepareRuntime 内重读 agent.yaml。
- 4 预设 enforced:true 后行为不变：validateMemoryConfig 对标准 6 层与各预设子集均 ok，仅误配（手改 authority_order 破坏不变量）才硬失败。
- memory-enforcement doctor warn（undefined/false）非 fail：旧 agent.yaml 与显式降级均可运行，仅告警，渐进迁移。

## 下一步

1. commit TASK-017（不 push）。
2. 等待用户授权下一批次（OP1 Stage B-E / OP3-A 长期 / OP5 Web 痕迹 / OP2-F / OP4-C）或转向其他工作。
