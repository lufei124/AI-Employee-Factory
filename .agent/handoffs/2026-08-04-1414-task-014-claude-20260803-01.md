# TASK-014 交接归档

状态：DONE
负责人：claude-20260803-01
完成时间：2026-08-04 14:14 +0800

## 完成内容

- OP4-D prune 分类（agentctl prune --logs/--registry-backups/--archives/--operations + 保留上限 + doctor 磁盘检查）：
  - `src/core/prune.ts`（新）：`PruneService` 中心化按分类清理，4 个 scope：
    - `logs`：遍历 `logsDir/<id>/runs/<slug>/`，按 slug 目录 mtime 判龄（`isDirectory()` 对 symlink 为 false → symlink 逃逸项在枚举阶段跳过），超 `keepDays` 删整个 slug 目录（`dirSize` 汇总字节），不动 `<id>` 根与 operations.jsonl。
    - `registry-backups`：列 `registryDir/backups/agents-*.yaml`，按 mtime 倒序保留最新 `keepCount` 份，余删除。
    - `archives`：列 `backupsDir/*.{tar.gz,.aief.enc,.enc}`，按 mtime 判龄删旧。
    - `operations`：`new OperationStore(logsDir).query({limit: MAX_SAFE_INTEGER})` 读全量，按 `started_at` 早于 cutoff 丢弃，`atomicWriteFile(file, content, 0o600)` 原子重写（全丢则写空串），保 0o600 且可再 query。
    - `safeRemove(root, candidate, label, dryRun)`：包 `assertInsideReal` + `fs.remove` 于 try/catch，越界或 symlink 逃逸项返回 `undefined`（跳过，不中止整批）；dry-run 只校验不删除。
    - `run(options)`：构建 scope 列表，空则抛 `VALIDATION_ERROR`；`dryRun` 透传；默认 keepDays logs/archives/operations=30/90/30、registry-backups keepCount=20。
  - `src/application/factory-application.ts`：增 `prune(options)` 薄编排，委托 `new PruneService(this.paths).run(options)`。
  - `src/cli-program.ts`：`registerPruneCommands(program)` + `prune` 单命令（scope flags `--logs/--registry-backups/--archives/--operations` + `--dry-run/--yes/--keep-days <number>/--keep-count <number>`）；非 dry-run 先 `prune({dryRun:true})` 预览 YAML，再 `confirmDanger`，再 `prune({dryRun:false})` 实跑并打印每 scope 绿色汇总 + 总释放字节；dry-run 仅预览。在 `:231-233` 注册组后调用。
  - `src/core/doctor.ts`：`disk-usage` 检查（warn）：`backupsDirSize()` 递归汇总 backupsDir 字节 + `countRunLogs()` 计 `logsDir/*/runs/*` 目录数，超 500MB 或 500 个则 warn，remediation 指向 `agentctl prune --dry-run`；插在 trash-health 之后、`if (agentId)` 之前。
- 明确不在本批（Forbidden）：skill/job per-workspace 归档清理、config 保留字段、preAction 自动 prune（区别于 trash 7 天自动 purge）、Stage B trace_id/ObservabilitySink、Stage C OTel GenAI、OP1/OP3-A/OP5、推送。

## 验证记录

- `npm run verify`：build 通过、124 项单测通过、lint 通过（prettier --write 修正 .agent/TASK_BOARD.md、src/cli-program.ts、src/core/prune.ts）。
- `npm run test:e2e`：1 项通过。
- 新增 tests/prune.test.ts(+8：logs/registry-backups/archives/operations 各一 + dry-run 零改动 + symlink 逃逸跳过保外部目标 + 无 scope 抛 VALIDATION_ERROR + 多 scope 组合)、doctor +1（disk-usage warn，稀疏 600MiB 文件触发）、cli-structure +1（断言 `prune`）。

## 备注

- 安全设计：prune 是破坏性操作，由 CLI 层 `--dry-run`/`--yes`/`confirmDanger` 把关，不在 preAction 自动跑；所有删除目标经 `assertInsideReal` 二次校验，symlink 逃逸项在枚举阶段（`isDirectory()`=false）即跳过，永不跟随删除受管根之外内容。
- operations 轮转用原子重写而非逐行删除，避免 jsonl 中间损坏；空结果写空串保文件存在与 0o600。
- 簿记沿用约定：本提交 TASK-014 行 Branch/worktree 用占位 `master (TASK-014 commit)`，下次任务提交回填实际 hash；本提交同时把 TASK-013 占位回填为 `8b17712`（TASK-014 开始时已写入，随本提交落盘）。

未 push（按规则任务完成即 commit，push 仍需用户明确授权）。
