# TASK-013 交接归档

状态：DONE
负责人：claude-20260803-01
完成时间：2026-08-04 13:20 +0800

## 完成内容

- OP4-A 可观测性（OperationStore + operations query + R12/R10）：
  - `src/core/secrets.ts`（新）：抽出共享 `SECRET_PATTERN`（AKIA/sk-/api_key/app_secret）与 `redactSecrets`，供 backup.ts（R27 备份扫描）与 operation-store.ts（摘要脱敏）复用，消除重复正则。
  - `src/core/operation-store.ts`（新）：`OperationStore` append-only 写 `logs/operations.jsonl`（`ensureDir 0o700` + `appendFile 0o600` + `chmod 0o600`）；`record()` 写 `OperationSummary`（error_message 经 `redactSecrets` 落为 `error_summary`）；`query()` 按 `agentId/kind/since/until/limit` 过滤，默认尾 100 条，文件不存在返回 `[]`。
  - `src/web/operation-manager.ts`：构造选项增 `store?: OperationStore`（字段 `OperationStore | undefined`，适配 `exactOptionalPropertyTypes`）；`execute()` 成功/失败终态 `await this.persist(operation)`，`finishCancelled()` 异步化并 `await persist`；`persist()` best-effort 写摘要（无 store 跳过，行为不变；startedAt/finishedAt 未就绪跳过）。所有取消路径 `return`，无双重持久化。
  - `src/web/server.ts`：默认 `new OperationManager({ store: new OperationStore(paths.logsDir) })`，保留 `options.operationManager` 注入口。
  - `src/core/config.ts`（R12）：`initializeFactory` chmod 0o700 列表补 `workspaceRoot/servicesDir/schedulesDir/logsDir/backupsDir`。
  - `src/services/launchd-service.ts`（R10）：`install()` 预创建 stdout/stderr 日志文件 0o600（已存在则 chmod 0o600 不截断，不存在则 `writeFile 0o600`）。
  - `src/application/factory-application.ts`：增 `queryOperations(filter)` 委托 `new OperationStore(paths.logsDir).query()`。
  - `src/cli-program.ts`：`operations` 命令组 + `query` 子命令（`--agent/--kind/--since/--until/--limit`，默认 100，YAML 输出）。
  - `src/core/backup.ts`：改为 `import { SECRET_PATTERN } from './secrets.js'`，删除本地正则常量。
- 明确不在本批（Forbidden）：Stage B ObservabilitySink/metadata.json trace_id、Stage C OTel、Stage D prune 分类、CLI 命令自身记录到 jsonl、jsonl 轮转/保留期、OP1/OP3-A/OP5。

## 验证记录

- `npm run verify`：build 通过、115 项单测通过、lint 通过。
- `npm run test:e2e`：1 项通过。
- 新增 tests/operation-store.test.ts(+4)、operation-manager +2、config +1、cli-structure +1（断言 `operations`/`query`）。

## 备注

- 测试时序修正：`cancelAll()` 须在被取消任务进入 running（`startedAt` 已写入）后调用，否则走「未启动即取消」分支（无 `startedAt`，`persist` 守卫跳过）；succeeded/failed 须先各自 `wait` 到终态再取消第三个。
- 簿记沿用约定：本提交 TASK-013 行 Branch/worktree 用占位 `master (TASK-013 commit)`，下次任务提交回填实际 hash；本提交同时回填 TASK-012 占位为 `c2a2b71`。

未 push（按规则任务完成即 commit，push 仍需用户明确授权）。
