# TASK-010 交接记录：实施记忆系统优化 OP0 + Phase 1 (OP2)

- Task ID: TASK-010
- Owner: claude-20260803-01
- 起止：2026-08-03 20:10 → 2026-08-04 11:11 +0800
- 分支：master（基线 commit 34a98b8，工作区未提交）
- 状态：DONE（改动未 commit，待用户授权）
- 研究蓝图：`.scratch/research/01-memory-system/05-synthesis/optimization-proposals.md` §OP2
- 计划文件：`.scratch/plan.md`

## 已实施

### OP0 - 记忆层演进闸门（决策+文档，无运行时代码）

- `docs/DECISIONS.md`：新增 D-009「封存不变量与版本化契约的演进流程」--frozen（语义固定、实现可经安全评审升级，如 assertInside realpath）vs versioned（env 白名单/config schema，经版本号 bump+显式分派）。
- `.scratch/research/01-memory-system/03-design-options/extension-surface.md` §2：增 frozen/versioned 分类补充（研究文档，gitignored）。

### OP2-A - 凭据维度隔离裂缝闭合

- **R2（P0）**：`src/core/job-runner.ts` scriptContext `buildSafeBaseEnvironment()` → `buildRuntimeEnvironment(agent)`；`src/services/factory-services.ts` jobLaunchdService env 注入 `...buildRuntimeEnvironment(agent)`（与 bridgeLaunchdService 对齐）。script Job 不再回退读 `~/.claude`。
- **R4（P0）**：`src/core/runtime.ts` `syncCcSwitchClaudeProvider` 增 `runtimesDir` 参数；CC Switch 源（`claude_config_dir` 解析后）若位于员工 Runtime Home 内则拒绝（防跨员工凭据复制），且源不得是软链接。`src/application/factory-application.ts` prepareRuntime 透传 `this.paths.runtimesDir`。
- **R24（P1，用户选 option-b）**：保留 3 个流量路由字段（BASE_URL/CUSTOM_HEADERS/USE_BEDROCK）可同步（兼容中继/代理型 Provider，D-006），但 `CcSwitchSyncSummary` 增 `routedFieldsChanged: string[]`（仅字段名，不记录值），prepareRuntime 在变更时 console.warn 告警。
- **R5**：延后（config.yaml 无 `sync` 段、无 zod config schema，需新增管道，低严重度）。

### OP2-B - assertInside realpath 升级 + 敏感入口补全

- `src/core/paths.ts`：新增 `assertInsideReal`（D-009 frozen 实现升级）--assertInside 语义之上补 fs.realpath 解析+软链接拒绝；不存在路径仅做同步包含校验（向后兼容配置期校验）。assertInside 签名/语义不变。
- 落地入口：ccSwitch 源（R4）、job-runner prompt/script 运行期复检（R18，scriptContext 改 async）、installSkill 源根符号链接拒绝（R6）、restoreBackupPath assertInsideReal(backupsDir)（R22）、scheduler.validate 异步化+assertInsideReal（R1）。

### OP2-C - 锁

- **R15 Registry 锁**：`src/core/registry.ts` 构造器增可选 `locksDir`；`update` 拆 `updateUnlocked` + `serialize`（FileLock fail-fast 上叠加有界重试，使 Web+CLI 并发写入排队而非立即 LOCKED）。`src/cli-program.ts:16` 透传 `paths.locksDir`。
- **R16 secureProfile 锁**：`src/application/factory-application.ts` 新增 `secureBridgeProfile` 私有助手（per-bridge `bridge-${id}.lock`），3 个既有调用点 + R14 新调用点统一经此（不改 BridgeAdapter，避免破坏 bridge-service.test）。
- **R17 FileLock 损坏拒绝**：`src/core/locks.ts` `readExisting` 区分 ENOENT（无锁）/瞬时半成品（50ms 重读）/持久损坏（抛 VALIDATION_ERROR+doctor 提示）。顶部增 PID 复用风险注释。

### OP2-D - Bridge 授权态统一

- **R14**：`bridgeStatus` exit 0 调 `secureBridgeProfile` 再 `markBridgeReady`（与 bridgeAuthorize 对齐，统一 authorization:ready 语义）。
- **R19**：`src/core/trash.ts` restore 置 `bridge.authorization: 'pending'`（与 BackupService.restore 一致，强制重新授权）。

## 改动文件清单

源码：`src/core/runtime.ts`、`src/core/paths.ts`、`src/core/registry.ts`、`src/core/bridge.ts`（未改，仅经应用层加锁）、`src/core/job-runner.ts`、`src/core/skills.ts`、`src/core/scheduler.ts`、`src/core/locks.ts`、`src/core/trash.ts`、`src/services/factory-services.ts`、`src/application/factory-application.ts`、`src/cli-program.ts`。
测试：`tests/runtime.test.ts`、`tests/paths.test.ts`、`tests/registry.test.ts`、`tests/locks.test.ts`、`tests/job-runner.test.ts`、`tests/skills.test.ts`、`tests/trash.test.ts`。
文档：`docs/DECISIONS.md`、`.scratch/research/.../extension-surface.md`（gitignored）、`.scratch/plan.md`（gitignored）。
簿记：`.agent/TASK_BOARD.md`、`.agent/FILE_LOCKS.md`、`.agent/PROJECT_STATE.md`、`.agent/task-ids/TASK-010/`。

## 验证（实跑结果）

- `npx tsc -p tsconfig.json --noEmit`：通过。
- `npm run verify`（build + vitest + lint）：build 通过；vitest 22 文件 89 项全过（含新增 11 项回归）；eslint --max-warnings=0 通过；prettier --check 通过。
- `npm run test:e2e`（playwright）：1 项通过（web-console.spec.ts）。
- 未虚报：失败项均如实修复（registry 并发锁先以 fail-fast 失败，改为 serialize 重试；readExisting 与并发半成品冲突，加 50ms 重读）。

## 延后项（后续任务）

- R3 HOME 隔离（P2，需 §4 评估方案 a/b；`tests/runtime.test.ts:51` HOME 断言未动）。
- R5 非 白名单 env 键清洗选项（需 config schema 管道）。
- R13 secureProfile 字段清洗（hooks/env/commands/mcpServers）、B5 secureProfile 解耦（P2）。
- OP2-E 备份 Secret 治理（P2）、OP2-F 扩展面能力隔离（P2）、OP3/4/5。
- 未单独写 R14/R16/R22 接线的应用层集成测试（需 execa/lark-channel-bridge 桩，过重）；底层原语 assertInsideReal/FileLock/secureProfile 行为均有单测覆盖，接线靠类型检查+评审。

## 回滚

逐项独立可回滚，无 schema 变更、无数据迁移。详见 `.scratch/plan.md` §9。

## 安全约束遵守

- 未读取/输出真实 API Key 或凭据值；测试均用占位串。
- 未修改用户 CC Switch Provider、真实 launchd 或飞书状态。
- 未 commit、未 push（待用户授权）。
- 未擦除 codex TASK-008 在 `src/core/runtime.ts`/`tests/runtime.test.ts` 的未提交成果，在其上叠加。
