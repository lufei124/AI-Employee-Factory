# 正式服 MySQL（只读账号；运维侧确认仅 SELECT 权限）
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_DATABASE=restart_life

# 兼容别名（与上行二选一即可，进程环境变量优先）
# MYSQL_HOST=
# MYSQL_PORT=3306
# MYSQL_USER=
# MYSQL_PASSWORD=
# MYSQL_DATABASE=restart_life

# App Store Connect API（可选；采集苹果应用商店评论时需要）
ASC_ISSUER_ID=
ASC_KEY_ID=
ASC_KEY_PATH=
